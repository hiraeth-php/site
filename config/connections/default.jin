[connection]

	;
	;
	;

	driver = env(DATABASES_DEFAULT_TYPE)

	;
	;
	;

	user = env(DATABASES_DEFAULT_USER)

	;
	;
	;

	pass = env(DATABASES_DEFAULT_PASS)

	;
	;
	;

	host = env(DATABASES_DEFAULT_HOST, 127.0.0.1)

	;
	;
	;

	port = env(DATABASES_DEFAULT_PORT)

	;
	;
	;

	name = env(DATABASES_DEFAULT_NAME, database)
